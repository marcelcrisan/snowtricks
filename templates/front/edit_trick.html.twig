{% extends 'base.html.twig' %}


{% block main %}

    <h1>Modification d'une figure !</h1>

    {{ form_start(formTrick) }}

        {{ form_row(formTrick.title) }}
        {# {{ form_row(formTrick.category) }} #}
        {{ form_row(formTrick.content) }}
        {{ form_row(formTrick.image) }}
        {{ form_row(formTrick.video) }}
        <button type="submit" class="btn btn-success">Modifier la figure</button>
        
        {{ form_rest(formTrick) }}

     {{ form_end(formTrick) }}

{% endblock %}