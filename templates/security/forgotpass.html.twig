
{% extends "base.html.twig" %}

{% block main %}
    <div class="container">
        <h1 class="form-title">Mot de passe oublié</h1>
        <form action="{{ path('forgot_pass') }}" method="post">
            <div class="form-group">
                <input placeholder="Nom d'utilisateur..." required name="_username" type="text" class="form-control">
            </div>
            <div class="form-group">
                <button type="submit" class="btn btn-primary mr-2 copyable">Envoyer !</button>
            </div>
        </form>
    </div>
{% endblock %}
